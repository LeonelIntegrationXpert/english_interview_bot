version: '3.1'
responses:
  utter_designcenter_raml_schema_import:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu importo schemas no Design Center usando include ou libraryImport.
                  Isso permite reutilizar contratos e tipos em vários projetos.
                  Garante validação automática de payloads conforme o esquema.
              - vr_en: |
                  I import schemas in Design Center using include or libraryImport.
                  This allows reusing contracts and types across multiple projects.
                  It ensures automatic payload validation against the schema.
