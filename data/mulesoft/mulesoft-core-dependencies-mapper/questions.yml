version: '3.1'
nlu:
  - intent: mulesoft-core-dependencies-mapper
    examples: |
      - pode contar do mulesoft-core-dependencies-mapper?
      - o que faz o core-dependencies-mapper?
      - explica o core-dependencies-mapper?
      - core-dependencies-mapper como funciona?
      - para que serve o mapeador de dependências?
      - can you explain mulesoft-core-dependencies-mapper?
      - what does core-dependencies-mapper do?
      - how does dependencies-mapper work?
      - can you talk about core dependencies mapper?
      - what is the dependencies mapper used for?
