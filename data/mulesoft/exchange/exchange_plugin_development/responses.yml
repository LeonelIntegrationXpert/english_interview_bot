version: '3.1'
responses:
  utter_exchange_plugin_development:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu uso Exchange SDK e publicação via Anypoint CLI para registrar extensoes.
                  Plugins podem adicionar menus, validações custom e integrações extras.
                  Debugging ocorre localmente antes de publicar no catalogo.
              - vr_en: |
                  I use the Exchange SDK and publish via Anypoint CLI to register extensions.
                  Plugins can add menus custom validations and extra integrations.
                  Debugging happens locally before publishing to the catalog.
