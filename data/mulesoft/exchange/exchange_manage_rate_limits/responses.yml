version: '3.1'
responses:
  utter_exchange_manage_rate_limits:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu defino rate limit via policy attach no Exchange.
                  Configuro chamadas por segundo e burst.
                  Exchange aplica throttling automaticamente.
              - vr_en: |
                  I set the rate limit via policy attach in Exchange.
                  I configure calls per second and burst.
                  Exchange enforces throttling automatically.
