version: '3.1'
responses:
  utter_exchange_api_key_management:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu gero API key unica por integracao e configuro scopes.
                  Posso revogar ou rotacionar keys via portal ou CLI.
                  Isso garante seguranca de automacoes externas.
              - vr_en: |
                  I generate a unique API key per integration and configure scopes.
                  I can revoke or rotate keys via portal or CLI.
                  This ensures security for external automations.
