version: '3.1'
responses:
  utter_exchange_import_asyncapi_spec:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu uso import e seleciono AsyncAPI para atualizacao.
                  Exchange valida topicos e esquemas.
                  Spec atualizada reflete no portal.
              - vr_en: |
                  I use import and select the AsyncAPI file for update.
                  Exchange validates topics and schemas.
                  The updated spec is reflected in the portal.
