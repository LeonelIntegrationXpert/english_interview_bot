version: '3.1'
responses:
  utter_exchange_distributed_architecture:
    - custom:
        response_array:
          -   - vr_pt: |
                  Exchange usa AWS global backbone para replicacao multi region.
                  Nodes sincronizam via event streams e caches locais.
                  Balanceadores globais redirecionam requests ao node mais proximo.
              - vr_en: |
                  Exchange leverages the global AWS backbone for multi region replication.
                  Nodes synchronize via event streams and local caches.
                  Global load balancers route requests to the nearest node.
