version: '3.1'
nlu:
  - intent: exchange_extension_parent_dependency
    examples: |
      - como childs dependem do parent
      - como gerenciar dependencias de inheritance
      - como evitar ciclo de dependencia
      - como checar dependencia hierarquica
      - como auditar dependencies de parent
      - how do children depend on the parent
      - how to manage inheritance dependencies
      - how to avoid dependency cycles
      - how to check hierarchical dependencies
      - how to audit parent dependencies
