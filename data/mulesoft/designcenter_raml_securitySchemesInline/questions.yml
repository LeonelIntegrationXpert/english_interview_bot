version: '3.1'
nlu:
  - intent: designcenter_raml_securitySchemesInline
    examples: |
      - como definir securitySchemes inline no RAML DC?
      - como usar esquemas de seguranca inline no DC?
      - pra que serve securitySchemes inline no RAML DC?
      - oque faz securitySchemes inline no DC?
      - como configurar securitySchemes inline no RAML DC?
      - how do you define inline securitySchemes in RAML in Design Center?
      - how do u use inline securitySchemes in DC?
      - what is inline securitySchemes used for in RAML in DC?
      - what does inline securitySchemes do in DC?
      - how do you configure inline securitySchemes in RAML in DC?
