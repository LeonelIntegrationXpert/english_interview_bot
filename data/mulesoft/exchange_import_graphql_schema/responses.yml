version: '3.1'
responses:
  utter_exchange_import_graphql_schema:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu uso import e escolho arquivo GraphQL para atualizar schema.
                  Exchange testa e mostra preview no portal.
                  Clientes veem nova estrutura de queries.
              - vr_en: |
                  I use import and choose the GraphQL file to update the schema.
                  Exchange tests and shows a preview in the portal.
                  Clients see the new query structure.
