version: '3.1'
responses:
  utter_exchange_convert_asset_to_template:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu seleciono convert e escolho template como tipo alvo.
                  Exchange gera estrutura padrao para novos projetos.
                  Acelera inauguracao de pipelines reutilizaveis.
              - vr_en: |
                  I select convert and choose template as target type.
                  Exchange generates a standard structure for new projects.
                  It accelerates bootstrap of reusable pipelines.
