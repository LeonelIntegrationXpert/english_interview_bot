responses:
  utter_extract_substring_dataweave:
    - custom:
        response_array:
          -   - vr_pt: |-
                  🔍 Utilize a função `substring()` para obter uma parte da string, especificando o índice inicial e o comprimento.
              - vr_en: |-
                  🔍 Use the `substring()` function to obtain part of the string by specifying the starting index and length.
