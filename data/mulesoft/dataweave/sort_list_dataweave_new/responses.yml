responses:
  utter_sort_list_dataweave_new:
    - custom:
        response_array:
          -   - vr_pt: |-
                  📚 Utilize `sortBy()` para ordenar a lista com base nos valores de cada item.
              - vr_en: |-
                  📚 Use `sortBy()` to sort the list based on the value of each item.
