responses:
  utter_string_to_number_dataweave:
    - custom:
        response_array:
          -
            - vr_pt: |
                🔢 *Conversão de String para Número no DataWeave*  
                Para transformar uma **string que representa um número** (ex: `"123"`) em um tipo numérico (`Number`), usamos o operador `as`.
                📌 **Exemplo simples:**  
                ```dw
                "123" as Number
                ```
                Isso é útil em situações onde os dados chegam como texto (ex: de um CSV ou JSON) e você precisa **fazer cálculos, comparações ou somas**.
                🧠 *Dica:* sempre verifique se a string tem apenas dígitos antes da conversão, para evitar erros em tempo de execução.
                ✅ *Resumo:*  
                Para converter string → número, use: `"valor" as Number`.

            - vr_en: |
                🔢 *Converting String to Number in DataWeave*  
                To convert a **string that contains a number** (e.g., `"123"`) into a numeric type (`Number`), use the `as` operator.
                📌 **Simple example:**  
                ```dw
                "123" as Number
                ```
                This is useful when the data comes as text (e.g., from CSV or JSON) and you need to **perform math operations or comparisons**.
                🧠 *Tip:* Always ensure the string contains only digits before converting, to avoid runtime errors.
                ✅ *Summary:*  
                To convert string → number, use: `"value" as Number`.
