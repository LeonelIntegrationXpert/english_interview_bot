responses:
  utter_map_vs_filter_usage:
    - custom:
        response_array:
          -
            - vr_pt: |
                🔄*Quando usar `map` ou `filter` no DataWeave*  
                🟩🇧🇷**Português: Qual a diferença entre `map` e `filter`?**  
                Ambas as funções percorrem listas, mas com propósitos diferentes:  
                ✅**`map`** é usada para *transformar* cada item de uma lista.  
                - Exemplo: converter nomes para letras maiúsculas.  
                - Resultado: a lista original permanece com o mesmo número de itens, mas transformados.  
                ✅**`filter`** é usada para *filtrar* itens com base em uma condição.  
                - Exemplo: manter apenas usuários com idade acima de 18 anos.  
                - Resultado: a lista pode ter menos itens, pois remove os que não atendem à condição.  
                🧠*Resumo:*  
                Use `map` quando você quiser **modificar os elementos** de uma lista.  
                Use `filter` quando você quiser **selecionar elementos com base em uma regra**.
            - vr_en: |
                🔄*When to use `map` or `filter` in DataWeave*  
                🗽🇺🇸**English: What's the difference between `map` and `filter`?**  
                Both functions iterate over lists, but serve different purposes:  
                ✅**`map`** is used to *transform* each item in a list.  
                - Example: convert names to uppercase.  
                - Result: same number of items, but transformed.  
                ✅**`filter`** is used to *select* items based on a condition.  
                - Example: keep only users over 18 years old.  
                - Result: fewer items may remain, depending on the condition.  
                🧠*Summary:*  
                Use `map` when you want to **modify the items** in a list.  
                Use `filter` when you want to **keep only items that match a rule**.
