responses:
  utter_handle_missing_fields_dataweave:
    - custom:
        response_array:
          -
            - vr_pt: |
                🧩 *Como lidar com campos ausentes no DataWeave?*  
                Durante uma transformação, campos esperados podem não estar presentes no `payload`. Para evitar falhas:
                ✅ **1. Use o operador `default`**  
                Define um valor padrão quando o campo está ausente.  
                Ex: `payload.nome default "Não informado"`
                ✅ **2. Combine `if`, `is null`, `!isEmpty`, `containsKey`**  
                Verificações úteis para validar a existência do campo.  
                Ex: `if (payload containsKey "idade") payload.idade else 0`
                ✅ **3. Use `try/catch` quando necessário**  
                Em cenários críticos, o bloco `try` captura exceções de campos ausentes.  
                Ex: `try payload.email catch "sem email"`
                📌 *Resumo:*  
                Sempre valide se os campos existem antes de utilizá-los, garantindo scripts mais robustos e sem erros de runtime.

            - vr_en: |
                🧩 *How to handle missing fields in DataWeave?*  
                Sometimes expected fields may be missing from the `payload`. To avoid runtime errors:
                ✅ **1. Use the `default` operator**  
                Provides a fallback when a field is not available.  
                Ex: `payload.name default "Not provided"`
                ✅ **2. Combine `if`, `is null`, `!isEmpty`, `containsKey`**  
                Useful checks to verify if the field exists.  
                Ex: `if (payload containsKey "age") payload.age else 0`
                ✅ **3. Use `try/catch` when needed**  
                In critical cases, `try` blocks help manage exceptions for missing fields.  
                Ex: `try payload.email catch "no email"`
                📌 *Summary:*  
                Always verify field existence before accessing it. This ensures robust and error-free DataWeave scripts.
