version: "3.1"

nlu:
  - intent: encode_payload_base64
    examples: |
      # 🇧🇷 Português
      - Como transformar um payload em base64 no DataWeave?
      - Como converter um conteúdo para base64 no DW?
      - Preciso codificar dados em base64 no DataWeave
      - Como gerar base64 a partir do payload?
      - Codificar payload como base64
      - Payload para base64 no MuleSoft, como fazer?
      - Quero usar base64 com DataWeave

      # 🇺🇸 English
      - How to convert a payload to base64 in DataWeave?
      - Encode payload to base64 using DW
      - How do I generate a base64 string from data in DataWeave?
      - I want to encode something as base64 in MuleSoft
      - DataWeave encode content to base64
      - base64 transformation in DW
      - Convert a value to base64 in MuleSoft
