responses:
  utter_convert_binary_to_string_dataweave:
    - custom:
        response_array:
          -   - vr_pt: |-
                  🔄 Use `as String` com especificação de encoding (por exemplo, UTF-8) para converter dados binários em string.
              - vr_en: |-
                  🔄 Use `as String` along with an encoding specification (e.g., UTF-8) to convert binary data into a string.
