version: '3.1'
responses:
  utter_exchange_policy_management:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu publico policies no Exchange e anexo via attach em APIs.
                  Users podem escolher politicas de rate limit, caching e segurança.
                  Exchange garante que politicas sejam aplicadas em tempo de execução.
              - vr_en: |
                  I publish policies in Exchange and attach them to APIs.
                  Users can select rate limit caching and security policies.
                  Exchange ensures policies are enforced at runtime.
