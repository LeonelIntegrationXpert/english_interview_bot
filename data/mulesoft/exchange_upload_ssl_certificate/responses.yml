version: '3.1'
responses:
  utter_exchange_upload_ssl_certificate:
    - custom:
        response_array:
          -   - vr_pt: |
                  Eu faço upload do .pem e key no portal de custom domains.
                  Exchange aplica TLS automaticamente.
                  Garantindo criptografia de ponta a ponta.
              - vr_en: |
                  I upload the .pem and key in custom domains portal.
                  Exchange applies TLS automatically.
                  It ensures end-to-end encryption.
