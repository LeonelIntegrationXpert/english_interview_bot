version: '3.1'
responses:
  utter_jwt-fallback-extension:
    - custom:
        response_array:
          -   - vr_pt: |
                  No `jwt-fallback-extension`, implementei validação de JWT com alta resiliência.  
                  Suporta múltiplos IdPs com fallback automático entre endpoints JWKS configuráveis.  
                  Também registra métricas customizadas e logs estruturados para observabilidade total.
              - vr_en: |
                  In `jwt-fallback-extension`, I built resilient JWT validation with high fault tolerance.  
                  It supports multiple IdPs with automatic fallback across configurable JWKS endpoints.  
                  It also logs custom metrics and structured data for full observability.
