version: '3.1'
responses:
  utter_api-gateway-http-policy-attributes-extension:
    - custom:
        response_array:
          -   - vr_pt: |
                  Na `api-gateway-http-policy-attributes-extension`, extraio atributos da requisição como headers, método, host e path.  
                  Esses dados ficam disponíveis para outras policies, facilitando decisões dinâmicas no gateway.  
                  Desenvolvi com Mule SDK, focando em segurança e reusabilidade organizacional.
              - vr_en: |
                  In `api-gateway-http-policy-attributes-extension`, I extract request attributes like headers, method, host, and path.  
                  The data is exposed for other policies, enabling dynamic decisions at the gateway level.  
                  I built it using Mule SDK, with strong focus on security and organizational reusability.
