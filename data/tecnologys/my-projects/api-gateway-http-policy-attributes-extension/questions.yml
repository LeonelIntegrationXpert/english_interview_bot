version: '3.1'
nlu:
  - intent: api-gateway-http-policy-attributes-extension
    examples: |
      - pode falar do api-gateway-http-policy-attributes-extension?
      - o que faz a extension de atributos?
      - explica a policy-attributes?
      - como funciona o attributes-extension?
      - para que serve essa extensão?
      - o que é essa extension de atributos na policy?
      - qual a utilidade do api-gateway-http-policy-attributes-extension?
      - como essa extensão impacta o runtime?
      - pra que usar attributes-extension numa custom policy?
      - ela altera os headers ou o comportamento da requisição?
      - can you explain api-gateway-http-policy-attributes-extension?
      - what does the attributes extension do?
      - how does policy-attributes work?
      - talk about the attributes-extension?
      - what is this extension used for?
      - what's the purpose of api-gateway-http-policy-attributes-extension?
      - how does it affect HTTP headers?
      - how is it used in custom policies?
      - does it modify the inbound request behavior?
      - what attributes can be injected with this extension?
