version: '3.1'
responses:
  utter_mule-http-policy-transform-extension:
    - custom:
        response_array:
          -   - vr_pt: |
                  Na `http-policy-transform-extension`, desenvolvi uma policy customizada de transformação HTTP.  
                  Ela manipula headers, paths e queryParams para enriquecer requisições e respostas no API Gateway.  
                  Usei Mule SDK para garantir performance máxima e controle preciso sobre o tráfego.
              - vr_en: |
                  In `http-policy-transform-extension`, I developed a custom HTTP transformation policy.  
                  It manipulates headers, paths, and queryParams to enrich requests and responses at the API Gateway.  
                  I used Mule SDK to ensure high performance and precise traffic control.
