version: '3.1'
responses:
  utter_universal-java-builder:
    - custom:
        response_array:
          -   - vr_pt: |
                  No `universal-java-builder`, criei uma CLI em Python para gerenciar ambientes Java e Maven.  
                  Alterno entre JDKs (oito, onze, dezessete e vinte e um) com configuração automática.  
                  Suporte a `--add-opens` e integração fácil em pipelines locais garantem flexibilidade total.
              - vr_en: |
                  In `universal-java-builder`, I built a Python CLI to manage Java and Maven environments.  
                  It switches between JDKs (eight, eleven, seventeen, and twenty-one) with auto setup.  
                  Support for `--add-opens` and seamless local pipeline integration ensure full flexibility.
