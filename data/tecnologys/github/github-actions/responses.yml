version: '3.1'
responses:
  utter_github-actions:
    - custom:
        response_array:
          -   - vr_pt: |
                  No pipeline `github-actions`, automatizo testes e deploy contínuo para MuleSoft:  
                  - executo MUnit com geração de cobertura e publicação no Anypoint Exchange.  
                  - faço deploy para CloudHub dois ponto zero e Runtime Fabric com controle de versão.  
                  - gerencio múltiplos ambientes (LTS e EDGE) com validações em paralelo.
              - vr_en: |
                  In the `github-actions` pipeline, I automate continuous testing and deployment for MuleSoft:  
                  - run MUnit with coverage reports and publish to Anypoint Exchange.  
                  - deploy to CloudHub two-point-zero and Runtime Fabric with version control.  
                  - manage multiple environments (LTS and EDGE) with parallel validations.
